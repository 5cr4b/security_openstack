<IfModule mod_evasive20.c>
    DOSHashTableSize    3097

# the number of requests allowed to the same page or URI at a particular page interval
    DOSPageCount        5

#the total number of requests by the same client 
    DOSSiteCount        5

    DOSPageInterval     1
    DOSSiteInterval     1

    DOSBlockingPeriod   1

    DOSEmailNotify      namvan.bui@dxc.com
    #DOSSystemCommand    "su - someuser -c '/sbin/... %s ...'"
    DOSLogDir           "/var/log/mod_evasive"
</IfModule>
